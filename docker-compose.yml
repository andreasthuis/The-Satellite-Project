services:
  redis:
    image: redis:7.4-alpine
    container_name: satellite-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --save
      - "60"
      - "1000"

volumes:
  redis-data:
